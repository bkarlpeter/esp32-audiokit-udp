# esp32-audiokit-udp
Let two audiokits exchange a audio stream. Master receives analog via line-in, slave gerates audio on line out. Include a functionality that kills latency and synchronises loudness when changed on master. Allow finetuning via serial input. 
